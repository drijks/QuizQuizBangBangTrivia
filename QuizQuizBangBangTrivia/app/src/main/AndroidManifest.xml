<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.drijks.quizquizbangbangtrivia">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".About"></activity>
        <activity android:name=".Options" />
        <activity android:name=".CategorySelect" />
        <activity android:name=".BangBangRound" />
        <activity android:name=".PlayerStats" />
        <activity android:name=".CategoryGame" />
        <activity android:name=".QuestionDetail" />
        <activity android:name=".ResultsItem" />
        <activity android:name=".Results" />
        <activity android:name=".Game" />
        <activity android:name=".TheGame" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>